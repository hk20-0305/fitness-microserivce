spring:
  application:
    name: ai-service

  config:
    import: configserver:${CONFIG_SERVER_URL:https://fitness-microserivce-1.onrender.com}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never